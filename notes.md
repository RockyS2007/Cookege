tests (pytest)
got to start doing documentation (try pylint, PythonTA lol)
docker file
load balancer
production and development databases
try to look into some AWS stuff and see how you can implement it

NEED TO SETUP: a database transaction so that a recipe insertion is all or nothing

TCP/UDP are network protocols to transfer data
The layer above that is what we are using (HTTP)
You can also look into:
    - Websockets (real time, bidirectional)
    - gRPC (high perf)
    - Webhooks (instead of client sending info, server sends info)

The color palette for this project can be found here: https://coolors.co/palette/03045e-023e8a-0077b6-0096c7-00b4d8-48cae4-90e0ef-ade8f4-caf0f8
The font family for this project can be found here: https://fonts.google.com/specimen/Sour+Gummy?preview.script=Latn 

Things for next time:
- Setup a damn venv for all python projects brotato (make sure you activate it then deactive it after installing dependencies)
- Make sure to start the React + Vite project BEFORE writing code